server:
  port: 8888

spring:
  application:
    name: paytrack-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations,classpath:/configurations/payment-service
  profiles:
    active: native

eureka:
  client:
    service-url:
      defaultZone: http://registry:8761/eureka