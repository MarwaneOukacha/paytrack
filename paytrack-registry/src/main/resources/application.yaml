server:
  port: 8761

spring:
  application:
    name: paytrack-registry

eureka:
  instance:
    hostname: paytrack-registry
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0